import styled from "styled-components";
import tw from "tailwind.macro";

export const Container = styled.div`
  ${tw`bg-gray-100 rounded leading-tight shadow hover:bg-gray-200 flex items-center px-2 py-2`}
`;
