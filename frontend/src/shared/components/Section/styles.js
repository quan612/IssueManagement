import styled from "styled-components";
import tw from "tailwind.macro";

export const SectionContainer = styled.div`
  margin-top: 0.7rem;
`;

export const SectionTitle = styled.div`
  ${tw` text-gray-600 text-md font-bold mb-2 mt-3 text-base`}
`;
